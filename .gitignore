node_modules
.next
out
build
.data
*.tsbuildinfo
next-env.d.ts
.env
.env*.local
.DS_Store
.vercel

# Firebase / GCP service-account keys — NEVER commit
*firebase-adminsdk*.json
*service-account*.json
*serviceAccount*.json

# all dotenv variants (secrets) except the example
.env.*
!.env.example
